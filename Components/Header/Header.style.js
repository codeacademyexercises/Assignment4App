import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  HeaderContainer: {
    height: 100,
    backgroundColor: '#4154AF',
    flexDirection: 'row',
    justifyContent: 'space-around',
  },
  Header: {
    color: 'white',
    fontSize: 20,
  },
});
